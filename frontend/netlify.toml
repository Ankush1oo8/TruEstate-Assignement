[build]
  base = "frontend"
  command = "npm install && npm run build"
  publish = "frontend/dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# You can set environment variables for Netlify builds in the Netlify UI.
# For local testing, create .env.production in the frontend folder or set VITE_API_BASE_URL in Netlify.
# Example: VITE_API_BASE_URL=https://your-backend-domain.com
